<h1>Editing Book</h1>
<%= form_for(@book) do |f|%>
  <%= f.text_field :title %>
  <%= f.text_area :opinion %>
  <%= f.submit "Update Book" %>
<% end %>

<%= link_to "Show", book_path(@book.id) %>
<%= link_to "Back", books_path %>
